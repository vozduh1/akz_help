🟠 Назначение: Отправка HTTP-запросов (GET, POST и т.д.)

🔘 Метод запроса — левее URL (выпадайка: GET, POST...)
🌐 URL — адрес API (например: https://example.com/api/user)
📤 Send — отправить запрос

📑 Params — GET-параметры (добавляются к URL, типа ?id=1)
📬 Headers — заголовки (например, Content-Type: application/json)
🧾 Body — тело запроса (используется в POST, PUT и т.д.)

🧰 Raw → JSON — выбирай при работе с JSON-данными
📥 Response — внизу, результат от сервера
📃 Status — HTTP-код ответа (200 = ок, 404 = не найден)

💾 Save — сохранить запрос в коллекцию
📦 Collections — папка со всеми сохранёнными запросами

🪛 New → Request → Создать новый запрос
